{
  "image": "mcr.microsoft.com/vscode/devcontainers/cpp:0-ubuntu-22.04",
  "features": {
    "ghcr.io/devcontainers/features/common-utils:2": {
      "installZsh": false,
      "installOhMyZsh": false,
      "upgradePackages": true,
      "username": "vscode",
      "uid": "1000",
      "gid": "1000"
    }
  },
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y libxmu-dev libxi-dev libgl-dev libxinerama-dev libxcursor-dev xorg-dev libglu1-mesa-dev pkg-config git curl zip unzip tar xserver-xorg-core xserver-xorg-input-all xserver-xorg-video-dummy xinit fluxbox tigervnc-standalone-server tigervnc-common novnc websockify && if [ -f \"${containerWorkspaceFolder}/vcpkg.json\" ]; then cd \"${containerWorkspaceFolder}\" && sed -i '/\"dx11-binding\",/d' vcpkg.json && sed -i '/\"win32-binding\",/d' vcpkg.json; fi && git clone https://github.com/microsoft/vcpkg ~/vcpkg_tool && ~/vcpkg_tool/bootstrap-vcpkg.sh && echo 'export VCPKG_ROOT=~/vcpkg_tool' >> ~/.bashrc && echo 'export VCPKG_DEFAULT_TRIPLET=x64-linux' >> ~/.bashrc && echo 'export PATH=~/vcpkg_tool:$PATH' >> ~/.bashrc && export VCPKG_ROOT=~/vcpkg_tool && export VCPKG_DEFAULT_TRIPLET=x64-linux && export PATH=~/vcpkg_tool:$PATH && if [ -f \"${containerWorkspaceFolder}/vcpkg.json\" ]; then ~/vcpkg_tool/vcpkg install --feature-flags=binarycaching,manifests --triplet ${VCPKG_DEFAULT_TRIPLET} \"--vcpkg-root=${VCPKG_ROOT}\" \"--x-manifest-root=${containerWorkspaceFolder}\" \"--x-install-root=${containerWorkspaceFolder}/vcpkg_installed\"; fi && echo \"--- VCPKG INSTALLATION DIAGNOSTICS ---\" && echo \"Listing contents of install root: ${containerWorkspaceFolder}/vcpkg_installed/${VCPKG_DEFAULT_TRIPLET}/ (if it exists)\" && ls -R \"${containerWorkspaceFolder}/vcpkg_installed/${VCPKG_DEFAULT_TRIPLET}/\" && echo \"Checking for glfw3 CMake files in ${containerWorkspaceFolder}/vcpkg_installed/${VCPKG_DEFAULT_TRIPLET}/:\" && find \"${containerWorkspaceFolder}/vcpkg_installed/${VCPKG_DEFAULT_TRIPLET}/\" -name \"*glfw3*Config.cmake\" -print && find \"${containerWorkspaceFolder}/vcpkg_installed/${VCPKG_DEFAULT_TRIPLET}/\" -name \"*GLFW3*Config.cmake\" -print && echo \"Checking for ImGui CMake files in ${containerWorkspaceFolder}/vcpkg_installed/${VCPKG_DEFAULT_TRIPLET}/:\" && find \"${containerWorkspaceFolder}/vcpkg_installed/${VCPKG_DEFAULT_TRIPLET}/\" -name \"*ImGui*Config.cmake\" -print && find \"${containerWorkspaceFolder}/vcpkg_installed/${VCPKG_DEFAULT_TRIPLET}/\" -name \"*imgui*Config.cmake\" -print && echo \"--- END VCPKG DIAGNOSTICS ---\" && mkdir -p ~/.vnc && echo 'password' | vncpasswd -f > ~/.vnc/passwd && chmod 600 ~/.vnc/passwd && echo '#!/bin/sh' > ~/.vnc/xstartup && echo 'unset SESSION_MANAGER' >> ~/.vnc/xstartup && echo 'unset DBUS_SESSION_BUS_ADDRESS' >> ~/.vnc/xstartup && echo 'xrdb $HOME/.Xresources' >> ~/.vnc/xstartup && echo 'fluxbox &' >> ~/.vnc/xstartup && chmod +x ~/.vnc/xstartup && echo 'export DISPLAY=:1' >> ~/.bashrc && vncserver :1 -localhost no -geometry 1280x720 -depth 24 -fg > /tmp/vncserver.log 2>&1 & && websockify --web /usr/share/novnc/ --heartbeat 30 6080 localhost:5901 > /tmp/websockify.log 2>&1 &",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-vscode.cpptools",
        "ms-vscode.cmake-tools"
      ]
    }
  },
  "containerEnv": {
    "DISPLAY": ":1"
  },
  "forwardPorts": [
    6080,
    5901
  ]
}
