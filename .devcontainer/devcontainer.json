{
  "image": "mcr.microsoft.com/vscode/devcontainers/cpp:0-ubuntu-22.04",
  "features": {
    "ghcr.io/devcontainers/features/common-utils:2": {
      "installZsh": false,
      "installOhMyZsh": false,
      "upgradePackages": true,
      "username": "vscode",
      "uid": "1000",
      "gid": "1000"
    }
  },
  "postCreateCommand": [
    "sudo apt-get update && sudo apt-get install -y build-essential libxmu-dev libxi-dev libgl-dev libxinerama-dev libxcursor-dev xorg-dev libglu1-mesa-dev pkg-config git curl zip unzip tar xserver-xorg-video-dummy xvfb x11vnc novnc websockify",
    "if [ -f \"/workspaces/imalgorithm/vcpkg.json\" ]; then cd \"/workspaces/imalgorithm\" && sed -i '/\"dx11-binding\",/d' vcpkg.json && sed -i '/\"win32-binding\",/d' vcpkg.json; fi",
    "(set -e; echo \"VCPKG_SETUP: Ensuring /home/vscode/.bashrc exists...\" && touch /home/vscode/.bashrc; echo \"VCPKG_SETUP: Removing existing vcpkg_tool directory if present...\" && rm -rf ~/vcpkg_tool)",
    "mkdir -p ~/.vnc",
    "echo 'password' | vncpasswd -f > ~/.vnc/passwd && chmod 600 ~/.vnc/passwd",
    "echo '#!/bin/sh' > ~/.vnc/xstartup && echo 'unset SESSION_MANAGER' >> ~/.vnc/xstartup && echo 'unset DBUS_SESSION_BUS_ADDRESS' >> ~/.vnc/xstartup && echo 'xrdb $HOME/.Xresources' >> ~/.vnc/xstartup && echo 'startxfce4 &' >> ~/.vnc/xstartup",
    "chmod +x ~/.vnc/xstartup",
    "grep -qxF 'export DISPLAY=:1' ~/.bashrc || echo 'export DISPLAY=:1' >> ~/.bashrc",
    "vncserver :1 -localhost no -geometry 1280x720 -depth 24 -fg > /tmp/vncserver.log 2>&1 &",
    "websockify --web /usr/share/novnc/ --heartbeat 30 6080 localhost:5901 > /tmp/websockify.log 2>&1 &"
  ],
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-vscode.cpptools",
        "ms-vscode.cmake-tools"
      ]
    }
  },
  "containerEnv": {
    "DISPLAY": ":1"
  },
  "forwardPorts": [
    6080,
    5901
  ]
}
