{
  "image": "mcr.microsoft.com/vscode/devcontainers/cpp:0-ubuntu-22.04",
  "features": {
    "ghcr.io/devcontainers/features/common-utils:2": {
      "installZsh": false,
      "installOhMyZsh": false,
      "upgradePackages": true,
      "username": "vscode",
      "uid": "1000",
      "gid": "1000"
    }
  },
"postCreateCommand": "sudo DEBIAN_FRONTEND=noninteractive apt-get update && sudo DEBIAN_FRONTEND=noninteractive apt-get install -y build-essential libxmu-dev libxi-dev libgl-dev libxinerama-dev libxcursor-dev xorg-dev libglu1-mesa-dev pkg-config git curl zip unzip tar xserver-xorg-core xserver-xorg-input-all xserver-xorg-video-dummy xinit fluxbox tigervnc-standalone-server tigervnc-common novnc websockify && if [ -f \"/workspaces/imalgorithm/vcpkg.json\" ]; then cd \"/workspaces/imalgorithm\" && sed -i '/\"dx11-binding\",/d' vcpkg.json && sed -i '/\"win32-binding\",/d' vcpkg.json; fi && (set -e; echo \"VCPKG_SETUP: Ensuring /home/vscode/.bashrc exists...\" && touch /home/vscode/.bashrc; echo \"VCPKG_SETUP: Removing existing vcpkg_tool directory if present...\" && rm -rf ~/vcpkg_tool && echo \"VCPKG_SETUP: Attempting to clone vcpkg...\" && git clone https://github.com/microsoft/vcpkg ~/vcpkg_tool && echo \"VCPKG_SETUP: vcpkg cloned successfully.\" && echo \"VCPKG_SETUP: Attempting to bootstrap vcpkg...\" && ~/vcpkg_tool/bootstrap-vcpkg.sh && echo \"VCPKG_SETUP: vcpkg bootstrapped successfully.\" && if [ ! -f ~/vcpkg_tool/vcpkg ]; then echo 'VCPKG_SETUP_ERROR: ~/vcpkg_tool/vcpkg (executable) not found after bootstrap! Something went wrong.'; exit 1; fi && if [ ! -f ~/vcpkg_tool/scripts/buildsystems/vcpkg.cmake ]; then echo 'VCPKG_SETUP_ERROR: ~/vcpkg_tool/scripts/buildsystems/vcpkg.cmake (toolchain file) not found after bootstrap! Something went wrong.'; exit 1; fi && grep -qxF 'export VCPKG_ROOT=~/vcpkg_tool' ~/.bashrc || echo 'export VCPKG_ROOT=~/vcpkg_tool' >> ~/.bashrc && grep -qxF 'export VCPKG_DEFAULT_TRIPLET=x64-linux' ~/.bashrc || echo 'export VCPKG_DEFAULT_TRIPLET=x64-linux' >> ~/.bashrc && grep -qxF 'export PATH=~/vcpkg_tool:$PATH' ~/.bashrc || echo 'export PATH=~/vcpkg_tool:$PATH' >> ~/.bashrc && export VCPKG_ROOT=~/vcpkg_tool && export VCPKG_DEFAULT_TRIPLET=x64-linux && export PATH=~/vcpkg_tool:$PATH && echo \"VCPKG_SETUP: Checking for vcpkg.json and attempting to install packages if present...\" && if [ -f \"/workspaces/imalgorithm/vcpkg.json\" ]; then ~/vcpkg_tool/vcpkg install --feature-flags=binarycaching,manifests --triplet ${VCPKG_DEFAULT_TRIPLET} \"--vcpkg-root=${VCPKG_ROOT}\" \"--x-manifest-root=/workspaces/imalgorithm\" \"--x-install-root=/workspaces/imalgorithm/vcpkg_installed\"; fi && echo \"VCPKG_SETUP: vcpkg package installation attempt finished.\" && echo \"--- VCPKG INSTALLATION DIAGNOSTICS ---\" && echo \"Listing contents of install root: /workspaces/imalgorithm/vcpkg_installed/${VCPKG_DEFAULT_TRIPLET}/ (if it exists)\" && ls -R /workspaces/imalgorithm/vcpkg_installed/${VCPKG_DEFAULT_TRIPLET}/ && echo \"Checking for glfw3 CMake files in /workspaces/imalgorithm/vcpkg_installed/${VCPKG_DEFAULT_TRIPLET}/:\" && find /workspaces/imalgorithm/vcpkg_installed/${VCPKG_DEFAULT_TRIPLET}/ -name \"*glfw3*Config.cmake\" -print && find /workspaces/imalgorithm/vcpkg_installed/${VCPKG_DEFAULT_TRIPLET}/ -name \"*GLFW3*Config.cmake\" -print && echo \"Checking for ImGui CMake files in /workspaces/imalgorithm/vcpkg_installed/${VCPKG_DEFAULT_TRIPLET}/:\" && find /workspaces/imalgorithm/vcpkg_installed/${VCPKG_DEFAULT_TRIPLET}/ -name \"*ImGui*Config.cmake\" -print && find /workspaces/imalgorithm/vcpkg_installed/${VCPKG_DEFAULT_TRIPLET}/ -name \"*imgui*Config.cmake\" -print && echo \"--- END VCPKG DIAGNOSTICS ---\") && mkdir -p ~/.vnc && echo 'password' | vncpasswd -f > ~/.vnc/passwd && chmod 600 ~/.vnc/passwd && echo '#!/bin/sh' > ~/.vnc/xstartup && echo 'unset SESSION_MANAGER' >> ~/.vnc/xstartup && echo 'unset DBUS_SESSION_BUS_ADDRESS' >> ~/.vnc/xstartup && echo 'xrdb $HOME/.Xresources' >> ~/.vnc/xstartup && echo 'fluxbox &' >> ~/.vnc/xstartup && chmod +x ~/.vnc/xstartup && grep -qxF 'export DISPLAY=:1' ~/.bashrc || echo 'export DISPLAY=:1' >> ~/.bashrc && vncserver :1 -localhost no -geometry 1280x720 -depth 24 -fg > /tmp/vncserver.log 2>&1 & ; websockify --web /usr/share/novnc/ --heartbeat 30 6080 localhost:5901 > /tmp/websockify.log 2>&1 &",  
"customizations": {
    "vscode": {
      "extensions": [
        "ms-vscode.cpptools",
        "ms-vscode.cmake-tools"
      ]
    }
  },
  "containerEnv": {
    "DISPLAY": ":1"
  },
  "forwardPorts": [
    6080,
    5901
  ]
}
